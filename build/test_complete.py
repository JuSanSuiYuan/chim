# Generated by Chim Compiler
# 启语(Chim)语言

import sys
import os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'runtime_py'))
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'compiler'))
from chim_runtime import send, recv

def 计算(a, b):
    # 返回类型: 整数
    _match_expr = a
    if _match_expr == 0:
        return b
    elif ((x := _match_expr) or True) and (x > 10):
        return a + b
    else:
        return a * b

def main():
    # 返回类型: 整数
    结果 = 计算(5, 3)
    return 结果
