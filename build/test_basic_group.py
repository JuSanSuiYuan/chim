# Generated by Chim Compiler
# 启语(Chim)语言

import sys
import os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'runtime_py'))
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'compiler'))
from chim_runtime import 输出, 范围, send, recv

def test_basic():
    # 组: 临时 (批量释放)
    try:
        x = 100
        输出(x)
    finally:
        pass  # 组 '临时' 结束，自动释放资源

def test_snap():
    # 返回类型: 整数
    result = 0
    # 组: 数据 (批量释放)
    try:
        data = [1, 2, 3]
        s = list(data) if isinstance(data, list) else data  # 快照
        result = s
    finally:
        pass  # 组 '数据' 结束，自动释放资源
    return result

def test_hdl():
    # 组: 外 (批量释放)
    try:
        buf = [1, 2]
        h = buf  # 句柄
        输出(h)
    finally:
        pass  # 组 '外' 结束，自动释放资源

def 主():
    test_basic()
    r = test_snap()
    输出(r)
    test_hdl()

if __name__ == '__main__':
    主()