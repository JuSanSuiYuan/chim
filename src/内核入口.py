# Generated by Chim Compiler
# 启语(Chim)语言

import chym.arch.x86_64.boot as x86
import chym.调度.调度器 as sched
import chym.用户.init as usr
import chym.io.serial as io

def 主():
    print('chym')
    x86.引导()
    usr.初始化()
    sched.运行()
