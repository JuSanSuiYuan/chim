# Match语句测试 - Swift风格但适配Chim

# 1. 基础值匹配
fn 测试基础匹配(数字: 整数):
    匹配 数字:
        案例 1 ->
            输出("一")
        案例 2 ->
            输出("二")
        案例 3 ->
            输出("三")
        _ ->
            输出("其他")

# 2. 元组模式匹配 (类似Swift)
fn 测试元组匹配(点: 元组):
    匹配 点:
        案例 (0, 0) ->
            输出("原点")
        案例 (x, 0) ->
            输出("在X轴")
        案例 (0, y) ->
            输出("在Y轴")
        _ ->
            输出("其他位置")

# 3. 范围匹配
fn 测试范围匹配(分数: 整数):
    匹配 分数:
        案例 90..100 ->
            输出("优秀")
        案例 80..90 ->
            输出("良好")
        案例 60..80 ->
            输出("及格")
        _ ->
            输出("不及格")

# 4. 带条件的模式 (guard - Swift风格)
fn 测试条件匹配(值: 整数):
    匹配 值:
        案例 x 当 x > 0 ->
            输出("正数")
        案例 x 当 x < 0 ->
            输出("负数")
        _ ->
            输出("零")

# 5. 布尔匹配
fn 测试布尔匹配(条件: 布尔):
    匹配 条件:
        案例 真 ->
            输出("真值")
        案例 假 ->
            输出("假值")
