// 测试简单函数定义
fn add(a: int, b: int) -> int = a + b

fn subtract(a: int, b: int) -> int:  
    return a - b

fn multiply(x: int, y: int) -> int {
    return x * y
}

// 测试基本变量和控制流
let age: int = 25
var name = "test"

if age >= 18:  
    print("adult")
else:  
    print("minor")

// 测试match表达式
match age:  
    25 => print("25 years old")
    18 => print("18 years old")
    _ => print("other age")

// 测试返回语句
return calculate_score(age)

fn calculate_score(score: int) -> int = score * 2
