// Basic Chim test - simple expressions

fn add(a: int, b: int) -> int = a + b;

fn multiply(x: int, y: int) -> int = x * y;

fn main() {
    let result: int = add(10, 20);
    let product: int = multiply(5, 6);
}
