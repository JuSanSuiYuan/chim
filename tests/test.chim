// 测试混合语法风格
fn add(a: int, b: int) -> int = a + b

fn subtract(a: int, b: int) -> int:  
    return a - b

fn multiply(x: int, y: int) -> int {
    return x * y
}

// 测试中文关键字
令 年龄: int = 25
设 姓名 = "张三"

如果 年龄 >= 18:  
    print("成年人")
否则:  
    print("未成年人")

// 测试match表达式
match 年龄:  
    25 => print("25岁")
    18 => print("18岁")
    _ => print("其他年龄")

// 测试结构体和枚举
struct Person {
    name: string
    age: int
    grade: float
}

enum Status {
    Loading
    Success(data: string)
    Error(message: string)
    Idle
}

let person = Person{name: "李四", age: 30, grade: 90.5}

// 测试返回语句
return calculate_score(年龄)

fn calculate_score(score: int) -> int = score * 2
