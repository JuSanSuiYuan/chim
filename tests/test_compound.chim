// 测试复合赋值操作符
fn test_compound_assign() -> int = {
    var x: int = 10
    x += 5  // 等价于 x = x + 5
    x -= 3  // 等价于 x = x - 3
    x *= 2  // 等价于 x = x * 2
    x /= 4  // 等价于 x = x / 4
    x %= 3  // 等价于 x = x % 3
    return x
}

let result = test_compound_assign()
print(result)
